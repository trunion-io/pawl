{
  "_comment": "Two endpoints, applied separately. See README.md — this file is a record, not a single PATCH body.",

  "permissions": {
    "enabled": true,
    "allowed_actions": "all",
    "sha_pinning_required": false
  },

  "permissions/workflow": {
    "default_workflow_permissions": "read",
    "can_approve_pull_request_reviews": false
  }
}
